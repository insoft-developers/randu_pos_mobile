import 'package:flutter/material.dart';
import '../../core/misc/app_image.dart';

class LogoApps extends StatelessWidget {
  const LogoApps({super.key});

  @override
  Widget build(BuildContext context) {
    return getPngImage('randu-logo');
  }
}
